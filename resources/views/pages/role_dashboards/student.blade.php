@extends('layouts.master')
@section('page_title', 'Student Dashboard')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h2 class="mb-1">Student dashboard</h2><p class="text-muted mb-0">{{ optional(optional($record)->user)->name ?: Auth::user()->name }} · {{ optional(optional($record)->my_class)->name }}</p></div>@if($record)<a href="{{ route('marks.year_selector', Qs::hash(Auth::user()->id)) }}" class="btn btn-primary"><i class="icon-book mr-1"></i>View report cards</a>@endif</div>
<div class="row"><div class="col-lg-8"><div class="card"><div class="card-header header-elements-inline"><h5 class="card-title">Recent results</h5>{!! Qs::getPanelOptions() !!}</div><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Subject</th><th>Exam</th><th>Mark</th><th>Grade</th></tr></thead><tbody>@forelse($marks as $mark)<tr><td>{{ optional($mark->subject)->name }}</td><td>{{ optional($mark->exam)->name }}</td><td>{{ $mark->cum_ave ?: $mark->cum }}</td><td>{{ optional($mark->grade)->name ?: 'Pending' }}</td></tr>@empty<tr><td colspan="4" class="text-center text-muted">No results have been published for {{ $session }}.</td></tr>@endforelse</tbody></table></div></div><div class="card"><div class="card-header"><h5 class="card-title">Recent attendance</h5></div><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Date</th><th>Status</th><th>Remarks</th></tr></thead><tbody>@forelse($attendance as $item)<tr><td>{{ optional($item->attendance_date)->format('d M Y') }}</td><td><span class="badge badge-{{ $item->status == 'present' ? 'success' : ($item->status == 'late' ? 'warning' : 'danger') }}">{{ ucfirst($item->status) }}</span></td><td>{{ $item->remarks ?: '—' }}</td></tr>@empty<tr><td colspan="3" class="text-center text-muted">No attendance records available.</td></tr>@endforelse</tbody></table></div></div></div><div class="col-lg-4"><div class="card"><div class="card-header"><h5 class="card-title">Fees for {{ $session }}</h5></div><div class="list-group list-group-flush">@forelse($fees as $fee)@php($balance = $fee->paid ? 0 : ($fee->balance ?: optional($fee->payment)->amount))<div class="list-group-item"><div class="d-flex justify-content-between"><span>{{ optional($fee->payment)->title }}</span><strong>{{ number_format($balance, 2) }}</strong></div><small class="text-muted">{{ $fee->paid ? 'Cleared' : 'Outstanding' }}</small></div>@empty<div class="list-group-item text-muted">No fee records available.</div>@endforelse</div></div><div class="card card-body bg-primary text-white"><h5>Keep learning</h5><p class="mb-0">Use your report card, timetable, and marksheet links to track your academic progress.</p></div></div></div>
@endsection
