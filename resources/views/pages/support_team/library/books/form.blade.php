@extends('layouts.master')
@section('page_title', $book->exists ? 'Edit Book' : 'Add Book')
@section('content')
<div class="card"><div class="card-header"><h5 class="card-title">{{ $book->exists ? 'Edit book' : 'Add book' }}</h5></div><div class="card-body"><form method="post" action="{{ $book->exists ? route('books.update', $book) : route('books.store') }}">@csrf @if($book->exists) @method('PUT') @endif<div class="row"><div class="col-md-6 form-group"><label>Book title</label><input name="name" required class="form-control" value="{{ old('name', $book->name) }}"></div><div class="col-md-6 form-group"><label>Author</label><input name="author" class="form-control" value="{{ old('author', $book->author) }}"></div><div class="col-md-4 form-group"><label>Book type</label><input name="book_type" class="form-control" value="{{ old('book_type', $book->book_type) }}"></div><div class="col-md-4 form-group"><label>Class</label><select name="my_class_id" class="form-control"><option value="">General</option>@foreach($classes as $class)<option value="{{ $class->id }}" {{ old('my_class_id', $book->my_class_id) == $class->id ? 'selected' : '' }}>{{ $class->name }}</option>@endforeach</select></div><div class="col-md-2 form-group"><label>Total copies</label><input type="number" min="0" required name="total_copies" class="form-control" value="{{ old('total_copies', $book->total_copies ?: 0) }}"></div><div class="col-md-2 form-group"><label>Issued</label><input type="number" min="0" name="issued_copies" class="form-control" value="{{ old('issued_copies', $book->issued_copies ?: 0) }}"></div><div class="col-md-6 form-group"><label>Location</label><input name="location" class="form-control" value="{{ old('location', $book->location) }}"></div><div class="col-md-6 form-group"><label>Catalogue URL</label><input type="url" name="url" class="form-control" value="{{ old('url', $book->url) }}"></div><div class="col-12 form-group"><label>Description</label><textarea name="description" class="form-control" rows="3">{{ old('description', $book->description) }}</textarea></div></div><button class="btn btn-primary">Save book</button> <a href="{{ route('books.index') }}" class="btn btn-light">Cancel</a></form></div></div>
@endsection
